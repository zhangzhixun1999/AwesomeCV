# AwesomeCV - 简历生成器

## 项目简介

AwesomeCV 是一个现代化的在线简历生成工具，帮助用户快速创建专业、精美的简历。支持多种模板风格，实时预览编辑，一键导出 PDF。

## 功能特性

- 📝 **多种精美模板**：现代、经典、创意三种风格模板
- ✏️ **实时预览编辑器**：左侧编辑，右侧实时预览
- 💾 **手动保存**：点击保存按钮保存修改
- 📄 **一键导出 PDF**：高质量 A4 格式 PDF 导出
- 🔐 **用户认证**：JWT Token 认证，数据安全保护
- 🎨 **精美 UI 设计**：基于 Tailwind CSS 的现代化界面

## 技术栈

### 前端
- **React 18.3.1** + TypeScript
- **TanStack React Query 5.28.0** - 数据 fetching
- **Tailwind CSS** - 样式框架
- **React Router 6** - 路由管理
- **html2pdf.js** - PDF 生成
- **Vite** - 构建工具

### 后端
- **FastAPI** (Python) - Web 框架
- **SQLAlchemy** - ORM
- **SQLite** - 数据库
- **JWT** - 用户认证

## 项目结构

```
resume/
├── frontend/                 # 前端项目（React + TypeScript）
│   ├── src/
│   │   ├── pages/           # 页面组件
│   │   │   ├── Home.tsx     # 首页
│   │   │   ├── Login.tsx    # 登录页
│   │   │   ├── Register.tsx # 注册页
│   │   │   ├── Dashboard.tsx# 我的简历
│   │   │   ├── Templates.tsx# 模板选择
│   │   │   └── Editor.tsx   # 编辑器
│   │   ├── components/      # UI组件
│   │   │   ├── ui/          # 基础UI组件
│   │   │   └── layout/      # 布局组件
│   │   ├── hooks/           # 自定义Hooks
│   │   │   ├── useAuth.ts
│   │   │   ├── useResumes.ts
│   │   │   ├── useTemplates.ts
│   │   │   └── useExportPDF.ts
│   │   └── utils/           # 工具函数
│   ├── package.json
│   └── vite.config.ts
│
└── backend/                 # 后端项目（FastAPI）
    ├── app/
    │   ├── routes/          # API路由
    │   │   ├── auth.py      # 认证API
    │   │   ├── resumes.py   # 简历API
    │   │   └── templates.py # 模板API
    │   ├── models/          # 数据模型
    │   │   ├── user.py
    │   │   └── resume.py
    │   ├── schemas/         # 数据验证
    │   │   ├── user.py
    │   │   └── resume.py
    │   ├── core/            # 核心配置
    │   │   ├── config.py
    │   │   └── security.py
    │   ├── db/              # 数据库
    │   │   ├── base.py
    │   │   └── session.py
    │   └── main.py          # 应用入口
    └── requirements.txt
```

## 快速开始

### 环境要求

- **Node.js** 18+
- **Python** 3.9+
- **npm** 或 **yarn**

### 1. 克隆项目

```bash
git clone <repository-url>
cd resume
```

### 2. 启动后端

```bash
cd backend

# 安装依赖
pip install -r requirements.txt

# 启动服务
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

后端将运行在：**http://localhost:8000**

API 文档：**http://localhost:8000/docs**

### 3. 启动前端

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端将运行在：**http://localhost:5173**

### 4. 访问应用

打开浏览器访问：**http://localhost:5173**

## 环境变量配置

### 后端（backend/.env）

创建 `backend/.env` 文件：

```bash
# JWT密钥（生产环境请使用随机字符串）
SECRET_KEY=your-secret-key-here-change-in-production

# 算法
ALGORITHM=HS256

# Token过期时间（分钟）
ACCESS_TOKEN_EXPIRE_MINUTES=1440
```

### 前端（frontend/.env）

创建 `frontend/.env` 文件：

```bash
# API地址
VITE_API_URL=http://localhost:8000
```

## 主要功能

### 1. 用户认证
- ✅ 注册新账号
- ✅ 登录/登出
- ✅ JWT Token 认证
- ✅ 路由保护

### 2. 简历管理
- ✅ 创建新简历
- ✅ 编辑简历内容
- ✅ 另存为副本
- ✅ 删除简历
- ✅ 导出 PDF

### 3. 模板选择
- 📦 **现代风格**：简洁现代，适合互联网行业
- 📄 **经典风格**：传统正式，适合传统行业
- ✨ **创意风格**：活泼创新，适合设计行业

### 4. 简历编辑
- 👤 **个人信息**：姓名、职位、联系方式
- 💼 **工作经历**：公司、职位、时间、描述
- 🎓 **教育背景**：学校、学位、专业、时间
- 🛠 **专业技能**：技能标签管理
- 📁 **项目经验**：项目名称、描述、技术栈

### 5. PDF 导出
- 📄 A4 纸张格式
- 🎨 高质量导出
- 🔍 缩放预览
- ⬇️ 一键下载

## 开发指南

### 前端开发

```bash
cd frontend

# 启动开发服务器（热重载）
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 代码检查
npm run lint
```

### 后端开发

```bash
cd backend

# 启动开发服务器（热重载）
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 启动生产服务器
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4
```

## API 接口

### 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/register` | 用户注册 |
| POST | `/api/auth/login` | 用户登录 |
| GET | `/api/auth/me` | 获取当前用户信息 |

### 简历接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/resumes` | 获取用户所有简历 |
| POST | `/api/resumes` | 创建新简历 |
| GET | `/api/resumes/:id` | 获取简历详情 |
| PUT | `/api/resumes/:id` | 更新简历 |
| DELETE | `/api/resumes/:id` | 删除简历 |
| POST | `/api/resumes/:id/duplicate` | 复制简历 |

### 模板接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/templates` | 获取所有模板 |
| GET | `/api/templates/:id` | 获取模板详情 |

## 部署

### 前端部署

```bash
cd frontend

# 构建生产版本
npm run build

# 将 dist/ 目录部署到静态服务器
# 例如：Nginx、Apache、Vercel、Netlify 等
```

### 后端部署

```bash
cd backend

# 安装依赖
pip install -r requirements.txt

# 启动服务（使用 Gunicorn 或 Uvicorn）
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

## 常见问题

### Q: 后端启动失败？

**A:** 请检查：
1. Python 版本是否为 3.9+
2. 是否安装了所有依赖：`pip install -r requirements.txt`
3. `.env` 文件是否存在且配置正确

### Q: 前端无法连接后端？

**A:** 请检查：
1. 后端服务是否已启动（访问 http://localhost:8000/docs）
2. `frontend/.env` 中的 `VITE_API_URL` 是否正确
3. 浏览器控制台是否有 CORS 错误

### Q: PDF 导出失败？

**A:** 请确保：
1. 浏览器支持 html2canvas（推荐使用 Chrome 或 Firefox）
2. 简历内容不为空
3. 网络连接正常

### Q: 如何修改模板？

**A:** 模板定义在 `backend/app/routes/templates.py` 中的 `TEMPLATES` 列表，你可以修改 `defaultContent` 来自定义模板内容。

### Q: 数据存储在哪里？

**A:** 默认使用 SQLite 数据库，文件位置：`backend/resume.db`。如需使用其他数据库，请修改 `backend/app/core/config.py` 中的 `DATABASE_URL`。

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！

## 联系方式

如有问题或建议，请提交 Issue 或联系维护者。
